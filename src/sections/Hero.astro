<section class="flex min-h-screen items-center px-4 pb-16 pt-28 sm:px-6 md:pb-20 md:pt-32">
  <div class="mx-auto grid max-w-7xl gap-10 md:grid-cols-2 md:items-center md:gap-12 lg:gap-16">

    {/* LEFT SIDE */}
    <div class="space-y-8 md:space-y-10">

      <h1 class="tracking-heading text-shadow-xs max-w-4xl text-3xl font-bold leading-tight sm:text-4xl lg:text-5xl">
          Tvořím firemní weby. <br>
          A hledám místa, kde klienti odcházejí.
      </h1>

      <p class="tracking-text text-shadow-xs max-w-2xl text-md leading-relaxed text-(--text-secondary) sm:text-md lg:text-xl">
        Navrhuji a tvořím firemní weby bez šablon a generického designu.
        Weby, které odpovídají značce, podporují důvěru a vedou klienta k rozhodnutí.
      </p>

      <p class="tracking-text text-shadow-xs max-w-2xl text-md leading-relaxed sm:text-md lg:text-xl">
        Pokud už web máte, pomohu odhalit místa, kde návštěvníci ztrácí zájem,
        důvěru nebo důvod ozvat se.
      </p>

      {/* BENEFITS */}
      <div class="grid max-w-xl gap-3 pt-4 sm:grid-cols-2">

        <div class="rounded-xl border border-(--muted) bg-(--muted)/50 px-4 py-3 font-serif tracking-serif text-md">
          Web na míru
        </div>

        <div class="rounded-xl border border-(--muted) bg-(--muted)/50 px-4 py-3 font-serif tracking-serif text-md">
          Design podle značky
        </div>

        <div class="rounded-xl border border-(--muted) bg-(--muted)/50 px-4 py-3 font-serif tracking-serif text-md">
          Větší důvěra klientů
        </div>

        <div class="rounded-xl border border-(--muted) bg-(--muted)/50 px-4 py-3 font-serif tracking-serif text-md">
          Jasná cesta k poptávce
        </div>

      </div>

      {/* CTA */}
      <div class="flex flex-col gap-3 pt-4 md:pt-6 sm:flex-row sm:flex-wrap">

        <a
          href="/portfolio"
          class="tracking-cta rounded-full bg-(--accent) px-6 py-3.5 text-center font-medium leading-relaxed text-(--background) transition hover:bg-(--accent-hover)"
        >
          Prohlédnout portfolio
        </a>

        <a
          href="https://calendly.com/mikulenka-info/45min"
          target="_blank"
          class="tracking-cta rounded-full border border-(--accent) px-6 py-3.5 text-center font-medium leading-relaxed transition hover:bg-(--accent)/10"
        >
          Diagnostika webu
        </a>

      </div>

      {/* TRUST TEXT */}
      <p class="max-w-xl text-sm text-(--text-secondary)">
        Design. Struktura. Důvěra.
        Od prvního dojmu až po rozhodnutí klienta.
      </p>

    </div>

    {/* RIGHT SIDE */}
    <div class="relative order-first hidden md:flex md:order-0 md:justify-center">

      <div class="flex w-full max-w-115 flex-col gap-6">

        {/* Workflow card */}
        <div class="rounded-4xl border border-(--muted) bg-(--background)/30 p-10 shadow-sm">

          <div class="flex flex-col items-center gap-4 text-center p-4">

            <div class="rounded-xl border border-(--muted) bg-(--background)/50 px-4 py-2 backdrop-blur-sm">
              Návštěvník
            </div>

            <div class="text-(--muted)">↓</div>

            <div class="rounded-xl border border-(--muted) bg-(--background)/50 px-4 py-2 backdrop-blur-sm">
              Web ( první dojem )
            </div>

            <div class="text-(--muted)">↓</div>

            <div class="rounded-xl border border-(--muted) bg-(--background)/50 px-4 py-2 backdrop-blur-sm">
              Zájem / důvěra
            </div>

            <div class="text-(--muted)">↓</div>

            <div class="rounded-xl border border-(--muted) bg-(--background)/50 px-4 py-2 backdrop-blur-sm">
              Získá poptávku
            </div>

            <div class="text-(--muted)">↓</div>

            <div class="rounded-xl border border-(--accent) bg-(--accent)/10 px-4 py-2">
              Audit → systém → rozhodnutí
            </div>

            <div class="text-(--muted)">↓</div>

            <div class="rounded-xl border border-(--muted) bg-(--background)/50 px-4 py-2 backdrop-blur-sm">
              Důvěra / spolupráce
            </div>

            {/* Positioning text */}

            <p class="mt-3 text-xl font-semibold text-(--text-primary)/85 font-serif tracking-serif-heading">
              Klienti neodpadávají náhodou.
            </p>
            <div class="my-3 h-px w-20 bg-(--muted)"></div>
            <p class="max-w-xs text-md leading-relaxed text-(--text-primary)/75">
                Nový web nebo diagnostika stávajícího.
                <br />
                Obojí začíná pochopením toho,
                jak vaši značku vnímá klient.
            </p>    
          </div>
        </div>
      </div>
    </div>
  </div>
</section>