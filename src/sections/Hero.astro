<section class="flex min-h-screen items-center px-4 pb-16 pt-28 sm:px-6 md:pb-20 md:pt-32">
  <div class="mx-auto grid max-w-7xl gap-10 md:grid-cols-2 md:items-center md:gap-12 lg:gap-16">

    {/* LEFT SIDE */}
    <div class="space-y-8 md:space-y-10">

      <h1 class="tracking-heading text-shadow-xs max-w-4xl text-4xl font-bold leading-tight sm:text-5xl lg:text-6xl">
        Přicházíte o klienty dřív, než vám vůbec napíšou.
      </h1>

      <p class="tracking-text text-shadow-xs max-w-2xl text-lg leading-relaxed text-(--text-secondary) sm:text-xl lg:text-2xl">
        Web, který nevede klienta k rozhodnutí, je jen drahá vizitka.
      </p>

      <p class="tracking-text text-shadow-xs max-w-2xl text-lg leading-relaxed sm:text-xl">
        Zjistím, proč vám klienti nepíšou — a co změnit jako první.
      </p>

      {/* BENEFITS */}
      <div class="grid max-w-xl gap-3 pt-4 sm:grid-cols-2">
        <div class="rounded-xl border border-(--muted) bg-(--muted)/50 px-4 py-3">
          Více kvalitních poptávek
        </div>

        <div class="rounded-xl border border-(--muted) bg-(--muted)/50 px-4 py-3">
          Méně chaosu v komunikaci
        </div>

        <div class="rounded-xl border border-(--muted) bg-(--muted)/50 px-4 py-3">
          Rychlejší rozhodování klientů
        </div>

        <div class="rounded-xl border border-(--muted) bg-(--muted)/50 px-4 py-3">
          Méně vysvětlování dokola
        </div>
      </div>

      {/* CTA */}
      <div class="flex flex-col gap-3 pt-4 md:pt-6 sm:flex-row sm:flex-wrap">
        <a
          href="https://calendly.com/mikulenka-info/45min"
          target="_blank"
          class="tracking-cta rounded-full bg-(--accent) px-6 py-3.5 text-center font-medium leading-relaxed text-(--background) transition hover:bg-(--accent-hover)"
        >
          Zjistit, kde mi utíkají klienti
        </a>

        <a
          href="#offer"
          class="tracking-text rounded-full border border-(--muted) px-6 py-3.5 text-center leading-relaxed text-(--accent) transition hover:border-(--accent) hover:text-(--text-primary)"
        >
          Jak audit funguje
        </a>
      </div>

      {/* TRUST TEXT */}
      <p class="max-w-xl text-sm text-(--text-secondary)">
        Bez chaosu. Prakticky a podle vašeho podnikání.
      </p>
    </div>

    {/* RIGHT SIDE */}
    <div class="relative order-first hidden md:flex md:order-0 md:justify-center">

      <div class="flex w-full max-w-115 flex-col gap-6">

        {/* Workflow card */}
        <div class="rounded-4xl border border-(--muted) bg-(--background)/30 p-10 shadow-sm">

          <div class="flex flex-col items-center gap-4 text-center p-4">

            <div class="rounded-xl border border-(--muted) bg-(--background)/50 px-4 py-2 backdrop-blur-sm">
              Návštěvník
            </div>

            <div class="text-(--muted)">↓</div>

            <div class="rounded-xl border border-(--muted) bg-(--background)/50 px-4 py-2 backdrop-blur-sm">
              Web ( první dojem )
            </div>

            <div class="text-(--muted)">↓</div>

            <div class="rounded-xl border border-(--muted) bg-(--background)/50 px-4 py-2 backdrop-blur-sm">
              Zájem / důvěra
            </div>

            <div class="text-(--muted)">↓</div>

            <div class="rounded-xl border border-(--muted) bg-(--background)/50 px-4 py-2 backdrop-blur-sm">
              Získá poptávku
            </div>

            <div class="text-(--muted)">↓</div>

            <div class="rounded-xl border border-(--accent) bg-(--accent)/10 px-4 py-2">
              Audit → systém → rozhodnutí
            </div>

            <div class="text-(--muted)">↓</div>

            <div class="rounded-xl border border-(--muted) bg-(--background)/50 px-4 py-2 backdrop-blur-sm">
              Důvěra / spolupráce
            </div>

            {/* Positioning text */}

            <p class="tracking-wider mt-3 text-xl font-semibold text-(--text-primary)/85 font-serif">
              Klienti neodpadávají náhodou.
            </p>
            <div class="my-3 h-px w-20 bg-(--muted)"></div>
            <p class="max-w-xs text-sm leading-relaxed text-(--text-secondary)">
              Jen někde ztrácíte důvěru.
              Zjistím kde — <br>
              a co změnit jako první.
            </p>    
          </div>
        </div>
      </div>
    </div>
  </div>
</section>