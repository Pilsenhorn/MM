<section id="solution" class="flex min-h-screen items-center px-4 py-28 sm:px-6 md:py-36">
  <div class="mx-auto max-w-7xl space-y-14 md:space-y-16">

    {/* Heading */}
    <div class="max-w-4xl space-y-5">

      <span class="inline-flex w-fit rounded-full border border-(--muted) px-4 py-1.5 text-sm text-(--text-secondary)">
        Ve většině případů není problém reklama
      </span>

      <h2 class="tracking-heading text-shadow-xs text-3xl font-bold leading-tight sm:text-4xl md:text-5xl">
        Proč se klienti neozývají nebo nerozhodnou?
      </h2>

      <p class="tracking-text max-w-3xl text-body font-medium leading-relaxed sm:text-lg text-(--text-secondary)">
        Často není problém v kvalitě služby.
        Ani v reklamě.
      </p>

      <p class="tracking-text max-w-3xl text-body font-medium leading-relaxed sm:text-lg">
        Problém bývá mezi prvním dojmem a rozhodnutím klienta.
      </p>

    </div>

    {/* Reasons */}
    <div class="grid gap-8 rounded-3xl border border-(--muted) bg-white/10 p-6 backdrop-blur-md md:grid-cols-3 sm:p-8">

      <div class="space-y-4 rounded-2xl border border-(--muted) p-5">

        <div class="flex h-12 w-12 items-center justify-center rounded-full border border-(--muted) text-lg font-bold text-(--accent)">
          01
        </div>

        <h3 class="tracking-heading text-xl font-semibold">
          Web neodpovídá na otázky
        </h3>

        <p class="tracking-text text-body">
          Klient neví cenu, proces, termín ani proč vybrat právě vás.
          A když musí přemýšlet moc dlouho — odejde.
        </p>
      </div>

      <div class="space-y-4 rounded-2xl border border-(--muted) p-5">

        <div class="flex h-12 w-12 items-center justify-center rounded-full border border-(--muted) text-lg font-bold text-(--accent)">
          02
        </div>

        <h3 class="tracking-heading text-xl font-semibold">
          Nikdo klienta nevede k rozhodnutí
        </h3>

        <p class="tracking-text text-body">
          Návštěvník přijde, rozhlédne se a odejde.
          Protože není jasné, co udělat dál.
        </p>
      </div>

      <div class="space-y-4 rounded-2xl border border-(--muted) p-5">

        <div class="flex h-12 w-12 items-center justify-center rounded-full border border-(--muted) text-lg font-bold text-(--accent)">
          03
        </div>

        <h3 class="tracking-heading text-xl font-semibold">
          Chybí důvěra
        </h3>

        <p class="tracking-text text-body">
          Klient musí mít pocit jistoty.
          Jinak si vás jen uloží „na později“ — a často už se nevrátí.
        </p>
      </div>
    </div>

    {/* Mechanism */}
    <div class="relative overflow-hidden rounded-3xl border border-(--muted) bg-white/10 p-6 backdrop-blur-md sm:p-8">

      <div class="absolute inset-0 bg-(--accent)/10 blur-3xl"></div>

      <div class="relative mx-auto max-w-4xl space-y-5 text-center">

        <p class="text-sm font-semibold uppercase tracking-[0.3em] text-(--accent)">
          Proto vzniká chaos
        </p>

        <h3 class="tracking-heading text-2xl font-semibold leading-snug sm:text-3xl">
          Web bez jasné cesty klienta je jen digitální vizitka.
        </h3>

        <p class="mx-auto max-w-2xl text-(--text-secondary)">
          Když klient nepochopí hodnotu, necítí důvěru a neví další krok,
          marketing přestane fungovat — <br>i když přivádí návštěvníky.
        </p>
      </div>
    </div>

    {/* CTA */}
    <div class="pt-4 text-center md:pt-6">
      <a
        href="#offer"
        class="tracking-cta inline-flex rounded-full bg-(--accent) px-8 py-4 text-body-lg font-medium leading-relaxed text-(--background) transition hover:bg-(--accent-hover)"
      >
        Ukázat mi, kde je problém
      </a>
    </div>

  </div>
</section>