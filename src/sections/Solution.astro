<section id="solution" class="flex min-h-screen items-center px-4 py-18 sm:px-6 md:py-24">
  <div class="mx-auto max-w-7xl space-y-12 md:space-y-16">
    <div class="space-y-4">
      <h2 class="tracking-heading text-shadow-sm max-w-4xl text-3xl font-bold leading-tight sm:text-4xl md:text-5xl">
        Váš web přestane být jen stránka.
        Stane se součástí systému, který vydělává.
      </h2>

      <p class="tracking-text max-w-3xl text-base font-medium leading-relaxed sm:text-lg">
        Ne další redesign.
        Ale systém, který vede klienta od prvního kontaktu až k rozhodnutí.
      </p>
      <p class="tracking-text max-w-3xl text-base font-medium leading-relaxed sm:text-lg">
        Web, obsah a poptávky musí fungovat jako jeden systém.
      </p>
    </div>

    <div class="grid gap-8 rounded-3xl border border-(--muted) bg-white/10 p-6 backdrop-blur-md md:grid-cols-3 sm:p-8">
      <div class="space-y-3 rounded-2xl border border-(--muted) p-4">
        <div class="flex h-12 w-12 items-center justify-center rounded-full border border-(--muted) text-lg font-bold text-(--accent)">01</div>
        <h3 class="tracking-heading text-xl font-semibold">Pochopím, kde vzniká chaos</h3>
        <p class="tracking-text text-sm sm:text-base">
          Zjistím, kde ztrácíte přehled a proč klienti nedochází k rozhodnutí.
        </p>
      </div>

      <div class="space-y-3 rounded-2xl border border-(--muted) p-4">
        <div class="flex h-12 w-12 items-center justify-center rounded-full border border-(--muted) text-lg font-bold text-(--accent)">02</div>
        <h3 class="tracking-heading text-xl font-semibold">Navrhnu strukturu systému</h3>
        <p class="tracking-text text-sm sm:text-base">
          Navrhnu jasnou cestu klienta a systému, který spojí web, obsah a poptávky.
        </p>
      </div>

      <div class="space-y-3 rounded-2xl border border-(--muted) p-4">
        <div class="flex h-12 w-12 items-center justify-center rounded-full border border-(--muted) text-lg font-bold text-(--accent)">03</div>
        <h3 class="tracking-heading text-xl font-semibold">Postavím systém, který vede k rozhodnutí</h3>
        <p class="tracking-text text-sm sm:text-base">
          Web, obsah i poptávky budou fungovat jako jeden celek.
          Bez chaosu. Bez zbytečných kroků.
        </p>
      </div>
    </div>

    <div class="relative overflow-hidden rounded-3xl border border-(--muted) bg-white/10 p-6 backdrop-blur-md sm:p-8">
      <div class="absolute inset-0 bg-(--accent)/10 blur-3xl"></div>

      <div class="relative space-y-4 pt-6">
        <p class="text-center text-sm font-semibold uppercase tracking-[0.3em] text-(--accent) sm:text-base">
          Výsledek
        </p>

        <h3 class="tracking-heading mx-auto max-w-4xl text-center text-2xl font-semibold leading-snug sm:text-3xl">
          Více poptávek. Méně chaosu. Systém, který vede klienty k rozhodnutí.
        </h3>
      </div>
    </div>

    <div class="pt-2 text-center md:pt-4">
      <a
        href="https://calendly.com/pilsenhorn/audit-systemu-30-min"
        target="_blank"
        class="tracking-cta block rounded-full bg-(--accent) px-8 py-4 font-medium leading-relaxed text-(--background) transition hover:bg-(--accent-hover)"
      >
         Navrhnout systém pro moje podnikání
      </a>
    </div>
  </div>
</section>
